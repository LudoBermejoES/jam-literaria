<div class="session-detail-container">
  <script>
    document.body.setAttribute('data-user-id', '<%= user.id %>');
  </script>
  <div class="session-header">
    <h2>Sesión: <span class="session-code"><%= session.code %></span></h2>
    <div class="session-status <%= session.status.toLowerCase() %>">
      <% if (session.status === 'WAITING') { %>
        Esperando
      <% } else if (session.status === 'COLLECTING_IDEAS') { %>
        Recolectando Ideas
      <% } else if (session.status === 'VOTING') { %>
        Votación
      <% } else if (session.status === 'REVOTING') { %>
        Desempate
      <% } else if (session.status === 'FINISHED') { %>
        Finalizada
      <% } %>
    </div>
  </div>
  
  <div class="session-content">
    <div class="session-sidebar">
      <div class="card participants-card">
        <div class="card-header">
          <h3>Participantes (<span id="participant-count"><%= participants.length %></span>)</h3>
        </div>
        <div class="card-body">
          <ul class="participants-list" id="participants-list">
            <% participants.forEach(function(participant) { %>
              <li class="participant <%= participant.id === user.id ? 'current-user' : '' %>" data-user-id="<%= participant.id %>">
                <%= participant.name %>
                <% if (participant.id === session.owner_id) { %>
                  <span class="badge owner-badge">Maestro</span>
                <% } %>
              </li>
            <% }); %>
          </ul>
        </div>
      </div>
      
      <div class="session-actions">
        <% if (isOwner) { %>
          <div class="share-section">
            <h4>Invitar Participantes</h4>
            <p>Comparte este código con otros para que se unan:</p>
            <div class="session-code-display">
              <%= session.code %>
              <button class="btn btn-sm" id="copy-code-btn" data-code="<%= session.code %>">Copiar</button>
            </div>
            <p class="mt-3">O comparte este enlace directo:</p>
            <div class="session-link-display">
              <input type="text" id="session-link" readonly value="<%= `${baseUrl}/session/join/${session.code}` %>" class="session-link-input" />
              <button class="btn btn-sm" id="copy-link-btn">Copiar</button>
            </div>
            
            <div class="session-actions mt-3">
              <button class="btn btn-danger" id="delete-session-btn" data-session-id="<%= session.id %>">
                Eliminar Sesión
              </button>
            </div>
          </div>
        <% } %>
      </div>
    </div>
    
    <div class="session-main">
      <% if (session.status === 'WAITING') { %>
        <div class="session-waiting card">
          <div class="card-header">
            <h3>Sala de Espera</h3>
          </div>
          <div class="card-body">
            <% if (isOwner) { %>
              <div class="owner-controls">
                <p>Como maestro de ceremonias, puedes iniciar la sesión cuando todos los participantes estén listos.</p>
                <p class="min-participants-note">Se necesitan al menos 2 participantes para comenzar.</p>
                <button 
                  id="start-session-btn" 
                  class="btn btn-primary" 
                  <%= participants.length < 2 ? 'disabled' : '' %>
                  data-session-id="<%= session.id %>"
                >
                  Iniciar Sesión
                </button>
              </div>
            <% } else { %>
              <div class="waiting-message">
                <p>Esperando a que el maestro de ceremonias inicie la sesión...</p>
                <div class="loader"></div>
              </div>
            <% } %>
          </div>
        </div>
      <% } else if (session.status === 'COLLECTING_IDEAS') { %>
        <!-- This will be implemented in the next phase -->
        <div class="card">
          <div class="card-body">
            <p>La sesión ha comenzado. ¡Es hora de compartir tus ideas!</p>
            <p>Esta sección se implementará en la siguiente fase.</p>
          </div>
        </div>
      <% } %>
    </div>
  </div>
</div>

<style>
  .session-detail-container {
    max-width: 1200px;
    margin: 0 auto;
  }
  
  .session-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1.5rem;
  }
  
  .session-code {
    font-family: monospace;
    font-weight: bold;
  }
  
  .session-content {
    display: flex;
    gap: 1.5rem;
  }
  
  .session-sidebar {
    width: 300px;
    flex-shrink: 0;
  }
  
  .session-main {
    flex-grow: 1;
  }
  
  .participants-card {
    margin-bottom: 1.5rem;
  }
  
  .participants-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  
  .participant {
    padding: 0.5rem 0;
    border-bottom: 1px solid var(--border-color);
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  
  .participant:last-child {
    border-bottom: none;
  }
  
  .current-user {
    font-weight: bold;
    color: var(--primary-color);
  }
  
  .badge {
    display: inline-block;
    padding: 0.25rem 0.5rem;
    border-radius: var(--border-radius);
    font-size: 0.75rem;
    font-weight: 600;
  }
  
  .owner-badge {
    background-color: var(--primary-color);
    color: white;
  }
  
  .session-code-display {
    background-color: #f8f9fa;
    padding: 0.75rem;
    border-radius: var(--border-radius);
    font-family: monospace;
    font-size: 1.1rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 0.5rem;
  }
  
  .session-link-display {
    background-color: #f8f9fa;
    padding: 0.75rem;
    border-radius: var(--border-radius);
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 0.5rem;
  }
  
  .session-link-input {
    border: none;
    background-color: transparent;
    flex-grow: 1;
    font-family: monospace;
    font-size: 0.85rem;
    margin-right: 0.5rem;
    width: 100%;
  }
  
  .mt-3 {
    margin-top: 1rem;
  }
  
  .btn-danger {
    background-color: #dc3545;
    color: #fff;
  }
  
  .btn-danger:hover {
    background-color: #bd2130;
  }
  
  .session-actions {
    margin-top: 1.5rem;
    padding-top: 1rem;
    border-top: 1px solid #dee2e6;
  }
  
  .min-participants-note {
    font-size: 0.875rem;
    color: #6c757d;
    margin-bottom: 1rem;
  }
  
  .waiting-message {
    text-align: center;
    padding: 2rem 0;
  }
  
  .loader {
    display: inline-block;
    width: 40px;
    height: 40px;
    margin-top: 1rem;
    border: 4px solid #f3f3f3;
    border-top: 4px solid var(--primary-color);
    border-radius: 50%;
    animation: spin 2s linear infinite;
  }
  
  @keyframes spin {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
  }
  
  @media (max-width: 768px) {
    .session-content {
      flex-direction: column;
    }
    
    .session-sidebar {
      width: 100%;
    }
  }
</style>

<script>
  // This will be replaced by session.js in the public folder
  document.addEventListener('DOMContentLoaded', () => {
    // Copy code button
    const copyBtn = document.getElementById('copy-code-btn');
    if (copyBtn) {
      copyBtn.addEventListener('click', () => {
        const code = copyBtn.getAttribute('data-code');
        navigator.clipboard.writeText(code)
          .then(() => {
            copyBtn.textContent = 'Copiado!';
            setTimeout(() => {
              copyBtn.textContent = 'Copiar';
            }, 2000);
          })
          .catch(err => {
            console.error('Error copying code:', err);
          });
      });
    }
  });
</script> 