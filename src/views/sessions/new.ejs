<div class="session-new-container">
  <div class="card">
    <div class="card-header">
      <h2>Crear Nueva Sesión</h2>
    </div>
    
    <div class="card-body">
      <p class="mb-3">Al crear una nueva sesión, te convertirás en el maestro de ceremonias.</p>
      
      <% if (error) { %>
        <div class="alert alert-error">
          <%= error %>
        </div>
      <% } %>
      
      <form action="/session/new" method="POST">
        <p class="info-text">
          Cuando crees la sesión, se generará automáticamente un código único que podrás compartir 
          con otros participantes para que se unan.
        </p>
        
        <div class="form-actions">
          <button type="submit" class="btn btn-primary">Crear Sesión</button>
          <a href="/session" class="btn">Cancelar</a>
        </div>
      </form>
    </div>
  </div>
</div>

<style>
  .session-new-container {
    max-width: 600px;
    margin: 2rem auto;
  }
  
  .mb-3 {
    margin-bottom: 1.5rem;
  }
  
  .info-text {
    background-color: #f8f9fa;
    padding: 1rem;
    border-radius: var(--border-radius);
    margin-bottom: 1.5rem;
    font-size: 0.95rem;
  }
  
  .form-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
  }
</style> 